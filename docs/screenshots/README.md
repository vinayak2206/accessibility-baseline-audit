# Screenshots

Put the three manual-verification screenshots described in
`../accessibility-audit-report.md` here:

- `lighthouse-score.png`
- `keyboard-focus-nav.png`
- `contrast-check.png`

This file exists only so the folder is tracked by git before those images
are added (git does not track empty directories).
